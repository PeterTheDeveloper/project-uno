import { Card } from './card';
export declare class Deck {
    private originalDraw;
    private shuffle;
    get cards(): Card[];
    get length(): number;
    constructor();
    draw(num?: number): Card[];
}
